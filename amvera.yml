name: jurconstructor-bot
runtime: nodejs18

include:
  - package.json
  - package-lock.json
  - src/
  - main.js
  - sync.js
  - .env
  - .dockerignore
  - amvera.yml

build:
  command: npm install

run:
  command: npm start
  scriptName: src/main.js

environment:
  NODE_ENV: production
